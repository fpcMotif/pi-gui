{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "Default capability set for the oh-my-pi webview. Grants the minimum permissions needed for the ACP-backed renderer: invoke core commands, subscribe to host events, read/write the clipboard, open file dialogs, post notifications, and receive updater signals.",
	"windows": ["main"],
	"permissions": [
		"core:default",
		"shell:default",
		"dialog:default",
		"notification:default",
		"clipboard-manager:default",
		"deep-link:default",
		"updater:default"
	]
}
